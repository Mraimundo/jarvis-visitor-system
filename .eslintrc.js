// Legacy ESLint config is intentionally disabled in favor of the flat config (eslint.config.mjs).
// Keeping this stub prevents conflicting rules between legacy and flat configurations.
// See BEST_PRACTICES.md for details.

module.exports = {};
